<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="standardFont">
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="labelFont" parent="@style/standardFont">
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_marginBottom">2dp</item>
    </style>

    <style name="signUpFont" parent="@style/standardFont">
        <item name="android:layout_marginBottom">20dp</item>
    </style>

    <style name="facebookLogo">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">30sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="loginInput">
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:background">#fff</item>
        <item name="android:padding">7dp</item>
    </style>

    <style name="loginButton" parent="@style/standardFont">
        <item name="android:background">#2e3d70</item>
        <item name="android:layout_margin">10dp</item>
    </style>

</resources>
